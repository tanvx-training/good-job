server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    config:
      uri: http://localhost:8888
      fail-fast: true
  config:
    import: "optional:configserver:"

logging:
  config: ${spring.cloud.config.uri}/api-gateway/logging/logback-spring.xml